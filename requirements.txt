# === Core ML (version theo đúng yêu cầu chính thức của Qwen3-VL-Embedding README) ===
torch==2.8.0
transformers>=4.57.0
qwen-vl-utils>=0.0.14
accelerate>=1.1.1

# Backend mặc định cho zero-config dev (EMBEDDING_MODEL_NAME=ViT-B-32) -- nhẹ,
# không cần checkpoint đặc biệt, tải tự động lần đầu chạy.
open-clip-torch>=2.29.0

# === Service ===
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0

# === Database clients (placeholder - xác nhận version thật khớp cluster DLong đang chạy) ===
pymilvus>=2.4.0
elasticsearch>=8.15.0
pymongo>=4.9.0

# === LLM API (Planner/Router/OCR-correction) ===
openai>=1.54.0
google-generativeai>=0.8.0

# === OCR / Object detection (xem câu hỏi context — cần xác nhận checkpoint) ===
paddleocr>=2.9.0
paddlepaddle-gpu  # bản GPU, cần đúng version CUDA của server (xem HUONG_DAN_DOCKER.md pattern cũ)
ultralytics>=8.3.0  # YOLO-World

# === Utils ===
opencv-python-headless>=4.10.0
tqdm>=4.67.0
python-dotenv>=1.0.1
